# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (Unix socket)
local   all             all                                     trust
local   replication     all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            trust
host    all             all             10.0.0.0/8              md5
host    all             all             172.16.0.0/12           md5
host    all             all             192.168.0.0/16          md5

# IPv6 local connections
host    all             all             ::1/128                 trust

# Docker network connections (adjust if needed)
host    all             all             0.0.0.0/0               md5

# Replication connections
# Allow replication user from Docker network
hostnossl replication replicator 0.0.0.0/0 md5
host    replication     replicator      0.0.0.0/0               md5
host    replication     replicator      127.0.0.1/32            trust
host    replication     replicator      ::1/128                 trust

# Allow regular database access for replicator user
host    all             replicator      0.0.0.0/0               md5